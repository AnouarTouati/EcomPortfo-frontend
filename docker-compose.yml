services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    image: react-vite
    container_name: react-vite
    volumes:
      - ./:/app:rw
    environment:
      NODE_ENV : "dev"
    ports:
      - "5173:5173"
      - "8080:80"
  
